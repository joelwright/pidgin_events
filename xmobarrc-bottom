Config { font = "-misc-fixed-*-*-*-*-10-*-*-*-*-*-*-*"
       , bgColor = "black"
       , fgColor = "grey"
       , lowerOnStart = True
       , commands = [ Run PipeReader "/home/$USER/.xmonad/pipes/status" "status"
                    , Run PipeReader "/home/$USER/.xmonad/pipes/messge" "messge"
                    , Run PipeReader "/home/$USER/.xmonad/pipes/change" "change"
                    ]
       , position = Bottom
       , sepChar = "%"
       , alignSep = "}{"
       , template = "%status% } %messge% { %change%"
       }
